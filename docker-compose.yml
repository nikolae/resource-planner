services:
  resource-planner:
    build: .
    image: resource-planner
    ports:
      - "5000:5000"
    volumes:
      - planner-data:/app/instance
      - ./config.json:/app/config.json
    restart: unless-stopped

volumes:
  planner-data:
